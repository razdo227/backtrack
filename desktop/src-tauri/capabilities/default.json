{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Default permissions for Backtrack",
  "windows": ["*"],
  "permissions": [
    "dialog:allow-open",
    "dialog:allow-save",
    "core:default",
    "shell:allow-open"
  ]
}
